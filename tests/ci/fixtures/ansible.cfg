[defaults]
# Inventory and repository paths are supplied explicitly by validate-ansible.sh.
# Do not inherit the deployment config or configure a Vault password source.
interpreter_python = auto_silent
host_key_checking = True
retry_files_enabled = False
