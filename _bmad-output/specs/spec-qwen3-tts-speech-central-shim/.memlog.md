---
topic: Local Qwen3-TTS Speech Central voice-mapping shim
updated: 2026-08-31T14:14
---

- (direction) Use express distillation from the conversation, the existing Qwen3-TTS design/playbook, Terraform host definition, host vars, and the live tts-shim Worker behavior read through Cloudflare MCP.
- (decision) Runtime path is Speech Central to a local shim on 192.168.1.247:8100 to the groxaxo Qwen3-TTS API on the same Compose network; Cloudflare Worker is not in the local runtime path.
- (decision) The local shim preserves the current Worker contract: case-insensitive OpenAI voice aliases map to model-specific voices, supported native Qwen voices may pass through, and unknown voices fall back to the alloy target.
- (decision) The initial Chinese mapping exposes Uncle_Fu, Dylan, Eric, Vivian, and Serena across Speech Central's fixed male and female OpenAI slots; final preference is chosen by user audition.
- (decision) The shim publishes host port 8100 while the groxaxo backend remains reachable only inside the Compose network on port 8880.
- (capability) CAP-1: Speech Central can call the local OpenAI-compatible TTS endpoint with its hardcoded voice names; success is playable Qwen3-TTS audio without client-side alias changes.
- (capability) CAP-2: Operators can configure OpenAI-to-Qwen voice mappings independently of the client; success is every supported Speech Central slot resolving to an enabled Qwen3 CustomVoice speaker.
- (capability) CAP-3: The shim preserves ordinary and streaming OpenAI-compatible TTS behavior; success is transparent request-field, status, content-type, and streamed-body forwarding with only voice rewritten.
- (capability) CAP-4: Ansible deploys and verifies the shim and Qwen3-TTS backend as one local service on llm-server; success is syntax validation plus health, models, WAV, streaming PCM, Compose, systemd, and restart-count checks.
- (capability) CAP-5: The user can audition Chinese voices in Speech Central and decide local usability; success is short-text and real-book playback with acceptable wait, continuity, and at least one acceptable voice.
- (capability) CAP-6: Operators can stop Qwen3-TTS without affecting Qwen3.6 and can keep DeepSeek mutually exclusive; success is the existing runtime boundary and rollback behavior remaining valid.
- (constraint) Target host is llm-server at 192.168.1.247 with the existing Terraform-managed VM, two passed-through RTX 3090 GPUs, and existing inventory identity; no Terraform resource change is required.
- (constraint) Retain Qwen/Qwen3-TTS-12Hz-1.7B-CustomVoice, pinned groxaxo source, optimized backend, GPU ordinal 1, one inference worker, one concurrent request, persistent model cache, and no boot enablement for the PoC.
- (constraint) Qwen3.6 must be active and DeepSeek mainline inactive before deployment; TTS must be stopped before DeepSeek starts.
- (constraint) Bind only to the trusted home LAN; do not add Cloudflare Worker, tunnel, public exposure, or plaintext credentials. Any later API key must use Ansible Vault indirection.
- (constraint) The shim must not buffer streaming audio and must not accept upstream URL overrides or model-routing overrides from client input.
- (constraint) Validation is local and non-deploying until explicit deployment authorization; repository workflow requires syntax-check before any targeted deployment and verify afterward.
- (assumption) For the LAN-only PoC, the shim remains unauthenticated like the current local design; Speech Central may send a dummy API key, and production authentication is deferred.
- (note) Wrapper-only content dropped: cloud GPU rental, M4 n=8 benchmark procedure, Cloudflare deployment instructions, and formal RTF/variance gates do not affect this local PoC contract.
- (note) Persistent fact file project-context.md was not present; repository AGENTS.md and cited brownfield sources supplied project constraints instead.
- (event) Coherence validation passed: six stable capabilities each have intent and success; constraints, non-goals, success signal, and the one inferred assumption satisfy Spec Law rules 1-6 and 8.
- (event) Preservation validation passed: local-only topology, Worker-derived mapping behavior, 192.168.1.247 deployment, existing Ansible/Terraform boundaries, user-experience acceptance, and explicit exclusions are preserved in SPEC.md or companions.
- (event) Spec finalized with six capabilities and three companions; artifacts remain under the repository-standard gitignored _bmad-output directory.
