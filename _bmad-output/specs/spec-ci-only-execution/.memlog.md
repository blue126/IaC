---
topic: CI-only Terraform and Ansible execution
updated: 2026-09-05T00:16
---

- (event) Reconstructed from committed architecture and learning note on 2026-09-05; the original ignored SPEC and append-only memlog were lost with a deleted worktree.
- (direction) Express reconstruction; preserve stable capability IDs CAP-1 through CAP-8.
- (capability) CAP-1: Gitea PR validation and protected-main deployment lanes; PR cannot mutate and only immutable approved main SHA may deploy.
- (capability) CAP-2: Jenkins executes Proxmox, ESXi, OCI, and NetBox Terraform roots against isolated named HCP workspaces without local state or concurrent writers.
- (capability) CAP-3: Jenkins deploys and verifies Ansible against refreshed exact scope with separate approval and a passing Verify contract.
- (capability) CAP-4: Typed audited maintenance workflows handle import, state, destroy, and manual playbooks without arbitrary command input.
- (capability) CAP-5: Authenticated replay-resistant NetBox events request allow-listed automation against immutable Gitea main.
- (capability) CAP-6: Gitea is sole writable authority and GitHub is a one-way mirror without infrastructure secrets or Jenkins triggers.
- (capability) CAP-7: Live NetBox data is adopted through restore-tested backup, ownership manifest, imports, zero unexplained drift, and reversible canary.
- (capability) CAP-8: Peer Gitea, Jenkins controller, and Jenkins agent LXCs are independently reproducible and recoverable with isolated identities and controlled bootstrap.
- (constraint) All managed-environment Terraform and Ansible execution is Jenkins-only except documented bootstrap or declared disaster recovery.
- (constraint) PR pipeline definition comes from protected main; iac-plan and iac-deploy are separate non-root agents with private workspaces and no shared writable path.
- (constraint) Terraform and Ansible use separate bound approvals, immutable manifests, stable conflict-domain locks, and fail-closed scope resolution.
- (constraint) Each Terraform root owns one named HCP workspace in local execution mode and validates state lineage before mutation.
- (constraint) Secrets, raw plans, tfvars, Vault passwords, OCI keys, and state backups are sensitive build-scoped material.
- (constraint) NetBox apply remains disabled until CAP-7 succeeds.
- (constraint) Controller rollback requires quiesced application-consistent restore-and-boot-tested backups; Gitea cutover requires fencing and latest-data rollback.
- (constraint) GitHub never triggers infrastructure jobs and stores no infrastructure secret.
- (decision) Non-goals: no cross-Proxmox-node HA, no HCP remote execution migration, no expansion of NetBox managed scope, and no external writes as PR validation.
- (question) May the same authorized homelab operator approve both Terraform and Ansible gates, or must approvers be distinct identities?
- (event) Self-Validate pass 1 coherence: PASS; Spec Law rules 1-6 and 8 satisfied for CAP-1 through CAP-8.
- (event) Self-Validate pass 2 preservation: PASS; load-bearing decisions remain discoverable through the adopted architecture and control-plane companions.
- (note) Wrapper-only content dropped: chat ceremony, reviewer scratch, and unrecoverable original memlog chronology.
- (event) Reconstructed spec finalized with one approval-identity open question.
