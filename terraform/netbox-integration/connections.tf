# Cable connections (disabled for VM interfaces)
# 说明: NetBox Provider 仅支持 `dcim.*`/`circuits.*` 对象作为 cable 终端，
# 虚拟机接口 `virtualization.vminterface` 不在允许列表内。
# 因此此处暂不对 VM/LXC eth0 建立 cable，后续改为以文档说明或自定义字段标注连接关系。

// intentionally left blank due to provider constraints

// intentionally left blank due to provider constraints

// intentionally left blank due to provider constraints

// intentionally left blank due to provider constraints
