---
# Default variables for anki-sync-server role
anki_port: 8080
anki_install_dir: /opt/anki-syncserver
anki_venv_path: "{{ anki_install_dir }}/venv"
# Sync users - format: "username:password"
# Override this in inventory with your actual credentials
anki_sync_users:
  - "user:pass"
